{
  "mcpServers": {
    "bylaw_retrieval": {
      "command": "${WORKTREE_ROOT}/.venv/bin/layer1-mcp",
      "args": ["--all-documents"],
      "env": {
        "DATABASE_URL": "${DATABASE_URL}"
      }
    }
  }
}
